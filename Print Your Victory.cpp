#include <iostream>

using namespace std;

int main() {
    // Standard I/O optimization (optional for this simple task)
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Print the required string exactly
    cout << "Red light. Green light. Accepted." << endl;

    return 0;
}